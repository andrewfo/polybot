Read the build plan at "POLYMARKET_BOT_PLAN (1).md", Section $ARGUMENTS.

Implement everything specified in that section completely:
1. Read the section's Context, Tasks, and Acceptance Criteria carefully
2. Create all files listed with full implementations — no stubs, no TODOs
3. Use exact LLM prompt templates from the plan without modification
4. Ensure all imports resolve and types are consistent with existing code
5. Write any tests specified in the acceptance criteria
6. Run the tests and fix failures before finishing

After implementation, verify every acceptance criterion is met and report which ones pass.
